# Dependency cooldown: never auto-install a package version published less than
# 7 days ago (10080 minutes), to avoid freshly-published / compromised releases.
# pnpm 10.x reads this from .npmrc. The committed pnpm-lock.yaml already pins
# exact versions, so installs stay reproducible regardless.
minimumReleaseAge=10080
